{
  "name": "pm-prioritization-tool",
  "version": "2.0.0",
  "private": true,
  "description": "Product Management Prioritization Tool (RICE, MoSCoW, financial frameworks) with browser cache and optional MongoDB on Vercel.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "npx --yes serve . -l 5173",
    "build": "node -e \"console.log('Static UI + Vercel serverless API (/api). Set MONGODB_URI on Vercel for cloud persistence.')\"",
    "start": "npm run dev"
  },
  "keywords": [
    "product-management",
    "prioritization",
    "rice",
    "moscow",
    "static-site"
  ],
  "dependencies": {
    "mongodb": "^6.16.0"
  }
}
